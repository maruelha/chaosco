# Pinned 2026-07-04 (refactoring step 1) — reproducible environment.
# When upgrading: change the pin, run the test suite, then commit.
flask==3.1.3
pandas==3.0.3
openpyxl==3.1.5
pyyaml==6.0.3
python-pptx==1.0.2

# dev / test
pytest==9.0.3
